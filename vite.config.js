import { defineConfig } from 'vite'

export default defineConfig({
  // Isso é necessário para o GitHub Pages encontrar seus arquivos se o repositório não for a página principal do seu perfil
  base: '/tracker-organizer/', 
})
